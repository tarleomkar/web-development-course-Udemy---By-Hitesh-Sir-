// && -> and (both true or false => true)
// || -> or (only one is true and other is fasle => true)
// ! -> reverse

let isLoggedIn = true;
let isPaid = true

console.log(isLoggedIn && isPaid); //true

isEmailUser = true
isGoogleUser = false

console.log(isEmailUser || isGoogleUser);

